{
  "name": "wutong-world",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html styles.css script.js favicon.svg CNAME .nojekyll dist/ && cp -R images resume works dist/",
    "dev": "python3 scripts/dev.py"
  }
}
