{
  "name": "wutong-world",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html styles.css script.js dist/ && cp -R public/* dist/",
    "dev": "python3 -m http.server 3000"
  }
}
